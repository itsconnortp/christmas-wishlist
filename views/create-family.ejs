<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Family - Christmas Wishlist</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <div class="container">
        <header>
            <h1>Christmas Wishlist</h1>
            <nav>
                <a href="/dashboard">Dashboard</a>
                <a href="/logout">Logout</a>
            </nav>
        </header>

        <div class="card" style="max-width: 600px; margin: 0 auto;">
            <h2>Create a New Family</h2>

            <% if (error) { %>
                <div class="error"><%= error %></div>
            <% } %>

            <form action="/family/create" method="POST">
                <div class="form-group">
                    <label for="familyName">Family Name</label>
                    <input type="text" id="familyName" name="familyName" placeholder="e.g., Smith Family Christmas 2025" required>
                </div>

                <p style="color: #666; margin-bottom: 20px;">
                    After creating your family, you'll get an invite code to share with your family members!
                </p>

                <button type="submit" class="btn btn-primary" style="width: 100%;">Create Family</button>
            </form>
        </div>
    </div>
</body>
</html>
